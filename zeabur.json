{
  "$schema": "https://schema.zeabur.app/template.json",
  "build": {
    "type": "static",
    "command": "npm run build",
    "output": "dist"
  },
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
